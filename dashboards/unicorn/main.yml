metric_keys:
  - unicorn_worker_count
dashboard:
  title: Unicorn
  description: |
    Track Unicorn worker counts with this dashboard.
  graphs:
    -
      title: 'Worker count'
      line_label: Workers
      kind: gauge
      format: number
      fields:
        - unicorn_worker_count
